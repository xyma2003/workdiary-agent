---
phase: 02-core-llm-nodes-and-template-routing
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - workdiary_agent/nodes/draft.py
  - workdiary_agent/nodes/polish.py
autonomous: true
requirements:
  - AGENT-06
  - AGENT-07
  - TMPL-01
  - TMPL-02
  - TMPL-03

must_haves:
  truths:
    - "draft_node reads template_type from state and generates a template-specific draft"
    - "draft_node includes '已选用XX模板' header in every draft (TMPL-02)"
    - "draft_node honours user-overridden template_type — if state has template_type='业务型', draft uses business template (TMPL-03)"
    - "polish_node refines the draft (not regenerates) with boss-perspective verbs"
    - "polish_node outputs '（未提供量化指标）' when no numbers are present in the input"
  artifacts:
    - path: "workdiary_agent/nodes/draft.py"
      provides: "Template-routed LLM draft generation"
      exports: ["draft_node"]
    - path: "workdiary_agent/nodes/polish.py"
      provides: "Boss-perspective polish LLM node"
      exports: ["polish_node"]
  key_links:
    - from: "workdiary_agent/nodes/draft.py"
      to: "workdiary_agent/state.py"
      via: "state.get('template_type')"
      pattern: "template_type"
    - from: "workdiary_agent/nodes/polish.py"
      to: "workdiary_agent/nodes/draft.py"
      via: "state.get('draft')"
      pattern: "draft"
---

<objective>
Replace draft_node and polish_node stubs with real LLM-backed implementations.
draft_node selects one of three hardcoded template structures (per D-05/06/07/08) based
on template_type in state, and includes "已选用XX模板" in the draft header (TMPL-02).
polish_node refines the draft using boss-perspective verbs and marks missing quantification
with "（未提供量化指标）" (D-09, D-10, AGENT-07).

Purpose: Produces the actual report draft and boss-friendly polished output (AGENT-06, AGENT-07).
Output: draft_node and polish_node produce real LLM-powered reports; template selection is
visible in output; user override of template_type is respected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-llm-nodes-and-template-routing/02-CONTEXT.md

<interfaces>
<!-- Critical patterns the executor must use exactly -->

From workdiary_agent/state.py:
```python
class StructuredInfo(BaseModel):
    tasks: list[str]
    outputs: list[str]
    blockers: list[str]
    progress: str

class AgentState(TypedDict, total=False):
    raw_input: str
    structured_info: Optional[StructuredInfo]
    template_type: Optional[str]   # "技术型" | "业务型" | "混合型"
    draft: Optional[str]
    polished: Optional[str]
```

Template structures (D-05, D-06, D-07 — hardcode in prompt, not files):
- 技术型: 任务 → 方案 → 进度 → 下一步
- 业务型: 结论 → 数据 → 进展 → 下一步
- 混合型: 业务影响 → 技术工作 → 量化指标 → 下一步

TMPL-02: draft must include "已选用XX模板" in the first line or header.
TMPL-03: draft_node reads template_type from state — does NOT hardcode a default template.
D-09: polish_node refines draft (takes draft as input), does NOT regenerate from scratch.
D-10: Missing quantification → insert "（未提供量化指标）" in the relevant section.
AGENT-07 goal-completion verbs: 完成、推进、对齐、输出、跟进
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement draft_node with 3-template system and TMPL-02 header</name>

  <read_first>
    - workdiary_agent/nodes/draft.py (current stub to replace)
    - workdiary_agent/state.py (StructuredInfo fields and AgentState schema)
    - tests/test_phase02_llm_nodes.py (test_draft_node_uses_template_type_from_state — what must pass)
    - .planning/phases/02-core-llm-nodes-and-template-routing/02-CONTEXT.md (D-05, D-06, D-07, D-08, TMPL-02, TMPL-03)
  </read_first>

  <behavior>
    - Input with template_type="技术型": draft contains "已选用技术型模板" and follows 任务→方案→进度→下一步 structure
    - Input with template_type="业务型": draft contains "已选用业务型模板" and follows 结论→数据→进展→下一步 structure
    - Input with template_type="混合型": draft contains "已选用混合型模板" and follows 业务影响→技术工作→量化指标→下一步 structure
    - draft is not "[stub draft]" and length > 20 characters
  </behavior>

  <files>workdiary_agent/nodes/draft.py</files>

  <action>
Replace the stub in workdiary_agent/nodes/draft.py with this implementation:

```python
# workdiary_agent/nodes/draft.py
"""
Draft node: generate a structured daily report draft based on StructuredInfo and template_type.

Three templates hardcoded in system prompts (D-05, D-06, D-07, D-08):
- 技术型: 任务 → 方案 → 进度 → 下一步
- 业务型: 结论 → 数据 → 进展 → 下一步
- 混合型: 业务影响 → 技术工作 → 量化指标 → 下一步

TMPL-02: Every draft begins with "【已选用XX模板】" for user visibility.
TMPL-03: template_type is always read from state — never hardcoded.
"""
from langchain_anthropic import ChatAnthropic
from langchain_core.messages import HumanMessage, SystemMessage

from ..state import AgentState


# ---------------------------------------------------------------------------
# Template-specific system prompts (D-05, D-06, D-07, D-08)
# ---------------------------------------------------------------------------

_TECH_SYSTEM = """你是一个技术日报撰写助手，采用「技术型」模板。
请按以下结构生成日报初稿：
1. 任务：今天进行了哪些技术工作
2. 方案：采用了什么技术方案或解决思路
3. 进度：当前进展状态
4. 下一步：明天计划的技术工作

要求：
- 第一行必须是：【已选用技术型模板】
- 每个章节用「**章节名**」加粗标注
- 语言简洁、专业
- 保持原始信息，不捏造细节"""

_BIZ_SYSTEM = """你是一个业务日报撰写助手，采用「业务型」模板。
请按以下结构生成日报初稿：
1. 结论：今天最重要的业务结果
2. 数据：支撑结论的关键数据或指标（若无数据请注明"（未提供量化指标）"）
3. 进展：各项业务事项的推进情况
4. 下一步：后续业务跟进计划

要求：
- 第一行必须是：【已选用业务型模板】
- 每个章节用「**章节名**」加粗标注
- 语言简洁、聚焦结果
- 保持原始信息，不捏造细节"""

_MIXED_SYSTEM = """你是一个混合型日报撰写助手，采用「混合型」模板。
请按以下结构生成日报初稿：
1. 业务影响：技术工作带来的业务价值
2. 技术工作：今天进行的技术实现细节
3. 量化指标：关键数据或度量（若无数据请注明"（未提供量化指标）"）
4. 下一步：业务与技术双线的后续计划

要求：
- 第一行必须是：【已选用混合型模板】
- 每个章节用「**章节名**」加粗标注
- 语言兼顾业务和技术受众
- 保持原始信息，不捏造细节"""

_TEMPLATE_PROMPTS = {
    "技术型": _TECH_SYSTEM,
    "业务型": _BIZ_SYSTEM,
    "混合型": _MIXED_SYSTEM,
}


def draft_node(state: AgentState) -> dict:
    """Generate report draft using the template selected by TemplateRouterAgent.

    Reads template_type from state (TMPL-03: respects user override).
    Returns draft string with '【已选用XX模板】' header (TMPL-02).
    """
    template_type = state.get("template_type", "混合型")
    structured_info = state.get("structured_info")
    raw_input = state.get("raw_input", "")

    # Build context for LLM
    if structured_info is not None:
        tasks_str = "\n".join(f"- {t}" for t in structured_info.tasks) or "（无）"
        outputs_str = "\n".join(f"- {o}" for o in structured_info.outputs) or "（无）"
        blockers_str = "\n".join(f"- {b}" for b in structured_info.blockers) or "（无）"
        progress_str = structured_info.progress or "（未提供）"
        context = (
            f"原始描述：{raw_input}\n\n"
            f"结构化信息：\n"
            f"任务：\n{tasks_str}\n"
            f"产出：\n{outputs_str}\n"
            f"问题/阻碍：\n{blockers_str}\n"
            f"整体进度：{progress_str}"
        )
    else:
        context = f"原始描述：{raw_input}"

    system_prompt = _TEMPLATE_PROMPTS.get(template_type, _MIXED_SYSTEM)

    llm = ChatAnthropic(model="claude-sonnet-4-5")
    response = llm.invoke([
        SystemMessage(content=system_prompt),
        HumanMessage(content=f"请根据以下信息生成日报初稿：\n\n{context}"),
    ])
    return {"draft": response.content}
```

Key points:
- `state.get("template_type", "混合型")` — respects user override (TMPL-03), defaults to 混合型
- Three separate system prompts (D-05/06/07/08), each hardcoded, not from files
- Each system prompt mandates "【已选用XX模板】" as first line (TMPL-02)
- "（未提供量化指标）" placeholder in 业务型 and 混合型 prompts (D-10 prep)
- Model "claude-sonnet-4-5" (locked per CLAUDE.md)
  </action>

  <verify>
    <automated>conda run -n llm-data-pipeline python -c "from workdiary_agent.nodes.draft import draft_node; print('import OK')"</automated>
  </verify>

  <acceptance_criteria>
    - workdiary_agent/nodes/draft.py contains "_TECH_SYSTEM", "_BIZ_SYSTEM", "_MIXED_SYSTEM" string variables
    - workdiary_agent/nodes/draft.py contains "_TEMPLATE_PROMPTS" dict
    - workdiary_agent/nodes/draft.py contains "state.get(\"template_type\""
    - workdiary_agent/nodes/draft.py contains "已选用技术型模板" in _TECH_SYSTEM
    - workdiary_agent/nodes/draft.py contains "已选用业务型模板" in _BIZ_SYSTEM
    - workdiary_agent/nodes/draft.py contains "已选用混合型模板" in _MIXED_SYSTEM
    - workdiary_agent/nodes/draft.py contains "ChatAnthropic(model=\"claude-sonnet-4-5\")"
    - The stub line `return {"draft": "[stub draft]"}` is no longer present
    - `from workdiary_agent.nodes.draft import draft_node` succeeds (no import error)
  </acceptance_criteria>

  <done>
    draft_node reads template_type from state, selects the matching system prompt from
    _TEMPLATE_PROMPTS, and returns a draft with "【已选用XX模板】" in the first line.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement polish_node with boss-perspective refinement</name>

  <read_first>
    - workdiary_agent/nodes/polish.py (current stub to replace)
    - workdiary_agent/state.py (AgentState schema — draft and polished fields)
    - tests/test_phase02_llm_nodes.py (test_polish_node_produces_boss_friendly_output — what must pass)
    - .planning/phases/02-core-llm-nodes-and-template-routing/02-CONTEXT.md (D-09, D-10, AGENT-07)
  </read_first>

  <behavior>
    - Input draft is not "[stub polished]" in result
    - result["polished"] contains at least one of: 完成、推进、对齐、输出、跟进
    - result["polished"] either contains a digit (quantified) or contains "未提供量化指标"
    - result["polished"] length > 20 characters
  </behavior>

  <files>workdiary_agent/nodes/polish.py</files>

  <action>
Replace the stub in workdiary_agent/nodes/polish.py with this implementation:

```python
# workdiary_agent/nodes/polish.py
"""
Polish node: refine the draft from boss-perspective without regenerating from scratch.

Strategy (D-09):
- INPUT: state["draft"] — the template-structured initial draft
- OUTPUT: polished version that leads with outcomes, uses goal-completion verbs
- Does NOT regenerate — improves tone/emphasis/verb choice of existing draft

AGENT-07 requirements:
- Lead with outcomes (business value first)
- Use goal-completion verbs: 完成、推进、对齐、输出、跟进
- Include quantified statements OR insert "（未提供量化指标）" placeholder (D-10)

D-10: If no numbers or metrics are found in the draft, insert "（未提供量化指标）" in the
     appropriate section rather than fabricating data.
"""
from langchain_anthropic import ChatAnthropic
from langchain_core.messages import HumanMessage, SystemMessage

from ..state import AgentState


_POLISH_SYSTEM = """你是一位资深职场写作顾问，专门从老板视角优化工作日报。

请对以下日报初稿进行润色改写，要求：
1. **保留模板结构**：保持章节框架不变，只优化语言表达
2. **成果优先**：每个章节开头先说结果，再说过程
3. **使用目标完成动词**：多用"完成"、"推进"、"对齐"、"输出"、"跟进"等动词
4. **量化表达**：
   - 如果原文有数字/指标，请在润色版中保留或突出展示
   - 如果原文没有数字/指标，在应有数据的位置插入"（未提供量化指标）"标注，不得捏造数据
5. **简洁专业**：语气正式但不啰嗦，老板一眼能看到重点

注意：不要添加原文中没有的事实信息。只改语气和表达方式。"""


def polish_node(state: AgentState) -> dict:
    """Refine draft with boss-perspective polish (AGENT-07).

    Reads state["draft"] and returns {"polished": improved_text}.
    Uses goal-completion verbs and inserts quantification placeholder if needed (D-09, D-10).
    """
    draft = state.get("draft", "")
    if not draft or draft == "[stub draft]":
        return {"polished": draft or ""}

    llm = ChatAnthropic(model="claude-sonnet-4-5")
    response = llm.invoke([
        SystemMessage(content=_POLISH_SYSTEM),
        HumanMessage(content=f"请润色以下日报初稿：\n\n{draft}"),
    ])
    return {"polished": response.content}
```

Key points:
- Takes `state.get("draft", "")` as input (D-09: refines, does not regenerate)
- System prompt explicitly requires 完成/推进/对齐/输出/跟进 verbs (AGENT-07)
- System prompt explicitly forbids fabricating data and requires "（未提供量化指标）" (D-10)
- Model "claude-sonnet-4-5" (locked per CLAUDE.md)
- Guards against empty/stub draft input
  </action>

  <verify>
    <automated>conda run -n llm-data-pipeline python -c "from workdiary_agent.nodes.polish import polish_node; print('import OK')"</automated>
  </verify>

  <acceptance_criteria>
    - workdiary_agent/nodes/polish.py contains "_POLISH_SYSTEM" string variable
    - workdiary_agent/nodes/polish.py contains "未提供量化指标" in the system prompt
    - workdiary_agent/nodes/polish.py contains all 5 goal verbs in the system prompt: "完成"、"推进"、"对齐"、"输出"、"跟进"
    - workdiary_agent/nodes/polish.py contains "state.get(\"draft\""
    - workdiary_agent/nodes/polish.py contains "ChatAnthropic(model=\"claude-sonnet-4-5\")"
    - The stub line `return {"polished": "[stub polished]"}` is no longer present
    - `from workdiary_agent.nodes.polish import polish_node` succeeds (no import error)
  </acceptance_criteria>

  <done>
    polish_node refines the input draft using boss-perspective system prompt, uses goal
    verbs, inserts quantification placeholder when needed. Import succeeds cleanly.
  </done>
</task>

</tasks>

<verification>
conda run -n llm-data-pipeline python -c "
from workdiary_agent.nodes.draft import draft_node
from workdiary_agent.nodes.polish import polish_node
print('All Phase 02-03 imports OK')
"
</verification>

<success_criteria>
- draft_node reads template_type from state and generates template-specific drafts
- draft_node includes "已选用XX模板" header in every output (TMPL-02)
- draft_node honours user-overridden template_type — each of the 3 types produces different output (TMPL-03)
- polish_node refines (not regenerates) the draft text
- polish_node system prompt mandates goal verbs and quantification placeholder
- All imports resolve without error
- graph.py (unchanged) still builds: `from workdiary_agent.graph import build_graph`
</success_criteria>

<output>
After completion, create .planning/phases/02-core-llm-nodes-and-template-routing/02-03-SUMMARY.md
</output>
