---
phase: 02-core-llm-nodes-and-template-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - workdiary_agent/nodes/extract.py
  - workdiary_agent/router/__init__.py
  - workdiary_agent/router/agent.py
  - workdiary_agent/nodes/route_template.py
autonomous: true
requirements:
  - AGENT-02
  - AGENT-05
  - TMPL-01
  - TMPL-02

must_haves:
  truths:
    - "extract_node calls ChatAnthropic with with_structured_output(StructuredInfo) and returns populated StructuredInfo"
    - "TemplateRouterAgent is a compiled sub-graph with nodes analyze_content and decide_template"
    - "route_template_node calls the compiled router sub-graph and writes template_type to state"
  artifacts:
    - path: "workdiary_agent/nodes/extract.py"
      provides: "Real LLM extraction node"
      exports: ["extract_node"]
    - path: "workdiary_agent/router/__init__.py"
      provides: "Router package init"
    - path: "workdiary_agent/router/agent.py"
      provides: "TemplateRouterAgent compiled sub-graph"
      exports: ["TemplateRouterAgent"]
    - path: "workdiary_agent/nodes/route_template.py"
      provides: "Main-graph node that invokes router sub-graph"
      exports: ["route_template_node"]
  key_links:
    - from: "workdiary_agent/nodes/route_template.py"
      to: "workdiary_agent/router/agent.py"
      via: "TemplateRouterAgent().invoke()"
      pattern: "TemplateRouterAgent"
    - from: "workdiary_agent/nodes/extract.py"
      to: "langchain_anthropic.ChatAnthropic"
      via: "with_structured_output(StructuredInfo)"
      pattern: "with_structured_output"
---

<objective>
Replace the extract_node stub with a real ChatAnthropic LLM call that returns a populated
StructuredInfo object. Create the TemplateRouterAgent sub-graph in workdiary_agent/router/
with two nodes (analyze_content → decide_template), then update route_template_node to invoke
the compiled sub-graph.

Purpose: Provides structured extraction (AGENT-02) and template routing (AGENT-05, TMPL-01).
Output: extract_node and route_template_node produce real LLM-powered results; TemplateRouterAgent
sub-graph compiled and accessible at workdiary_agent.router.agent.TemplateRouterAgent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-llm-nodes-and-template-routing/02-CONTEXT.md
@.planning/phases/02-core-llm-nodes-and-template-routing/02-01-SUMMARY.md

<interfaces>
<!-- Critical patterns the executor must use exactly -->

From workdiary_agent/state.py:
```python
class StructuredInfo(BaseModel):
    tasks: list[str] = Field(default_factory=list, description="Tasks worked on today")
    outputs: list[str] = Field(default_factory=list, description="Tangible outputs produced")
    blockers: list[str] = Field(default_factory=list, description="Blockers or issues encountered")
    progress: str = Field(default="", description="Overall progress summary")

class AgentState(TypedDict, total=False):
    raw_input: str
    structured_info: Optional[StructuredInfo]
    template_type: Optional[str]   # "技术型" | "业务型" | "混合型"
```

LangGraph sub-graph pattern (per CLAUDE.md tech stack):
```python
from langgraph.graph import StateGraph, START, END
from typing_extensions import TypedDict

class RouterState(TypedDict, total=False):
    raw_input: str
    structured_info_summary: str   # intermediate analysis
    template_type: str

router_builder = StateGraph(RouterState)
router_builder.add_node("analyze_content", analyze_content_node)
router_builder.add_node("decide_template", decide_template_node)
router_builder.add_edge(START, "analyze_content")
router_builder.add_edge("analyze_content", "decide_template")
router_builder.add_edge("decide_template", END)
router_graph = router_builder.compile()
```

ChatAnthropic with_structured_output (per CLAUDE.md tech stack):
```python
from langchain_anthropic import ChatAnthropic
from langchain_core.messages import HumanMessage, SystemMessage

llm = ChatAnthropic(model="claude-sonnet-4-5")
structured_llm = llm.with_structured_output(StructuredInfo)
result: StructuredInfo = structured_llm.invoke([SystemMessage(...), HumanMessage(...)])
```

Node signature (per existing patterns):
```python
def xxx_node(state: AgentState) -> dict:
    raw = state.get("raw_input", "")
    ...
    return {"field": value}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement extract_node with real ChatAnthropic structured output</name>

  <read_first>
    - workdiary_agent/nodes/extract.py (current stub to replace)
    - workdiary_agent/state.py (StructuredInfo fields and AgentState schema)
    - tests/test_phase02_llm_nodes.py (test_extract_node_returns_structured_info — what must pass)
  </read_first>

  <behavior>
    - Test input: AgentState with raw_input="今天完成了用户登录模块的单元测试，修了3个bug，还和产品对齐了需求，明天要做数据库迁移"
    - Expected: result["structured_info"] is StructuredInfo instance
    - Expected: info.tasks is non-empty list of strings
    - Expected: info.outputs is non-empty list of strings
    - Expected: info.blockers is a list (may be empty)
    - Expected: info.progress is non-empty string
  </behavior>

  <files>workdiary_agent/nodes/extract.py</files>

  <action>
Replace the stub in workdiary_agent/nodes/extract.py with this implementation:

```python
# workdiary_agent/nodes/extract.py
"""Extract node: parse raw Chinese work description into StructuredInfo via LLM."""
from langchain_anthropic import ChatAnthropic
from langchain_core.messages import HumanMessage, SystemMessage

from ..state import AgentState, StructuredInfo


_SYSTEM_PROMPT = """你是一个工作日报信息提取助手。请从用户的口语化工作描述中，提取以下结构化信息：

- tasks: 今天实际进行的工作任务列表（每项是简短的动宾短语，如"完成用户登录模块单元测试"）
- outputs: 产出的具体成果或交付物列表（如"通过3个bug修复"、"需求对齐确认"）
- blockers: 遇到的阻碍或未解决问题列表（若无则为空列表）
- progress: 一句话总结今天整体工作进展（如"主要功能开发推进顺利，无重大阻碍"）

请只提取用户描述中明确提及的信息，不要臆测或补充未提及的内容。"""


def extract_node(state: AgentState) -> dict:
    """Call ChatAnthropic with_structured_output to parse raw_input into StructuredInfo."""
    raw_input = state.get("raw_input", "")
    if not raw_input:
        return {"structured_info": StructuredInfo()}

    llm = ChatAnthropic(model="claude-sonnet-4-5")
    structured_llm = llm.with_structured_output(StructuredInfo)

    messages = [
        SystemMessage(content=_SYSTEM_PROMPT),
        HumanMessage(content=f"请提取以下工作描述的结构化信息：\n\n{raw_input}"),
    ]
    result: StructuredInfo = structured_llm.invoke(messages)
    return {"structured_info": result}
```

Key points:
- Model is "claude-sonnet-4-5" (locked per CLAUDE.md)
- Uses `state.get("raw_input", "")` — never `state["raw_input"]` (total=False TypedDict)
- Returns early with empty StructuredInfo() if raw_input is empty
- System prompt in Chinese matching the app's domain
- `with_structured_output(StructuredInfo)` returns StructuredInfo directly (not a dict)
  </action>

  <verify>
    <automated>conda run -n llm-data-pipeline python -c "from workdiary_agent.nodes.extract import extract_node; print('import OK')"</automated>
  </verify>

  <acceptance_criteria>
    - workdiary_agent/nodes/extract.py contains "with_structured_output(StructuredInfo)"
    - workdiary_agent/nodes/extract.py contains "ChatAnthropic(model=\"claude-sonnet-4-5\")"
    - workdiary_agent/nodes/extract.py contains "state.get(\"raw_input\""
    - workdiary_agent/nodes/extract.py contains "_SYSTEM_PROMPT" string variable
    - `from workdiary_agent.nodes.extract import extract_node` succeeds (no import error)
    - The stub line `return {"structured_info": None}` is no longer present
  </acceptance_criteria>

  <done>
    extract_node calls ChatAnthropic.with_structured_output(StructuredInfo) and returns
    {"structured_info": StructuredInfo(...)} with real extracted data. Import succeeds cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TemplateRouterAgent sub-graph and update route_template_node</name>

  <read_first>
    - workdiary_agent/nodes/route_template.py (current stub to replace)
    - workdiary_agent/state.py (AgentState schema — template_type field)
    - tests/test_phase02_llm_nodes.py (test_router_classifies_templates and test_route_template_node_sets_template_type — what must pass)
    - workdiary_agent/graph.py (confirms route_template_node is called with full AgentState)
  </read_first>

  <behavior>
    - Tech input classify result == "技术型"
    - Business input classify result == "业务型"
    - Mixed input classify result == "混合型"
    - route_template_node result["template_type"] is one of {"技术型", "业务型", "混合型"}
  </behavior>

  <files>
    workdiary_agent/router/__init__.py,
    workdiary_agent/router/agent.py,
    workdiary_agent/nodes/route_template.py
  </files>

  <action>
**Step 1: Create workdiary_agent/router/__init__.py**

```python
# workdiary_agent/router/__init__.py
"""TemplateRouterAgent — compiled LangGraph sub-graph for template classification."""
from .agent import TemplateRouterAgent

__all__ = ["TemplateRouterAgent"]
```

**Step 2: Create workdiary_agent/router/agent.py**

```python
# workdiary_agent/router/agent.py
"""
TemplateRouterAgent: compiled sub-graph that classifies a work description into one of
three template types: 技术型 (technical), 业务型 (business), 混合型 (mixed).

Sub-graph topology: START → analyze_content → decide_template → END
Uses independent RouterState (does not share AgentState).
"""
from __future__ import annotations

from typing_extensions import TypedDict
from langchain_anthropic import ChatAnthropic
from langchain_core.messages import HumanMessage, SystemMessage
from langgraph.graph import StateGraph, START, END


class RouterState(TypedDict, total=False):
    """Independent state for the TemplateRouterAgent sub-graph."""
    raw_input: str
    structured_info_text: str   # textual summary of structured_info for classification
    content_features: str       # intermediate: key content features extracted in analyze step
    template_type: str          # output: "技术型" | "业务型" | "混合型"


_ANALYZE_SYSTEM = """你是一个内容分析助手。请分析工作描述，提取以下特征：
1. 是否包含技术细节（代码、架构、数据库、API、算法、测试等）
2. 是否包含业务内容（客户、GMV、合同、需求对齐、数据报表、商务等）
3. 主要工作类型比例

请用简洁的1-2句话描述内容特征，供后续分类使用。"""

_DECIDE_SYSTEM = """你是一个日报类型分类助手。根据内容特征，从以下三种类型中选择最合适的一种：
- 技术型：工作内容以技术实现为主（编码、架构设计、测试、Bug修复等），业务内容较少
- 业务型：工作内容以业务推进为主（客户对齐、数据分析、需求确认、商务谈判等），技术内容较少
- 混合型：技术工作和业务工作均有实质性内容，缺一不可

请只输出以下三者之一（不含其他文字）：技术型、业务型、混合型"""


def analyze_content_node(state: RouterState) -> dict:
    """Analyze work content and extract classification features."""
    raw = state.get("raw_input", "")
    si_text = state.get("structured_info_text", "")
    combined = f"{raw}\n\n结构化摘要：{si_text}" if si_text else raw

    llm = ChatAnthropic(model="claude-sonnet-4-5")
    response = llm.invoke([
        SystemMessage(content=_ANALYZE_SYSTEM),
        HumanMessage(content=f"请分析以下工作内容的特征：\n\n{combined}"),
    ])
    return {"content_features": response.content}


def decide_template_node(state: RouterState) -> dict:
    """Classify template type based on extracted content features."""
    features = state.get("content_features", "")
    raw = state.get("raw_input", "")

    llm = ChatAnthropic(model="claude-sonnet-4-5")
    response = llm.invoke([
        SystemMessage(content=_DECIDE_SYSTEM),
        HumanMessage(content=f"内容特征：{features}\n\n原始描述：{raw}"),
    ])
    # Normalize — strip whitespace, fall back to 混合型 if unexpected value
    raw_type = response.content.strip()
    valid_types = {"技术型", "业务型", "混合型"}
    template_type = raw_type if raw_type in valid_types else "混合型"
    return {"template_type": template_type}


# ---------------------------------------------------------------------------
# Compile the sub-graph once at module level
# ---------------------------------------------------------------------------
_builder = StateGraph(RouterState)
_builder.add_node("analyze_content", analyze_content_node)
_builder.add_node("decide_template", decide_template_node)
_builder.add_edge(START, "analyze_content")
_builder.add_edge("analyze_content", "decide_template")
_builder.add_edge("decide_template", END)

_router_graph = _builder.compile()


class TemplateRouterAgent:
    """Wrapper around the compiled router sub-graph.

    Usage:
        router = TemplateRouterAgent()
        template_type = router.classify("today's work description...")
        # Returns: "技术型" | "业务型" | "混合型"
    """

    def classify(self, raw_input: str, structured_info_text: str = "") -> str:
        """Classify raw_input into a template type.

        Args:
            raw_input: User's work description (口语化 Chinese text)
            structured_info_text: Optional textual summary of StructuredInfo for richer context

        Returns:
            One of "技术型", "业务型", "混合型"
        """
        result = _router_graph.invoke({
            "raw_input": raw_input,
            "structured_info_text": structured_info_text,
        })
        return result.get("template_type", "混合型")
```

**Step 3: Replace workdiary_agent/nodes/route_template.py**

```python
# workdiary_agent/nodes/route_template.py
"""
Route template node: calls TemplateRouterAgent compiled sub-graph to classify
the work description and writes template_type into AgentState.
"""
from ..state import AgentState
from ..router.agent import TemplateRouterAgent


def route_template_node(state: AgentState) -> dict:
    """Invoke TemplateRouterAgent sub-graph to determine template_type.

    Reads raw_input and optional structured_info from state.
    Returns {"template_type": "技术型" | "业务型" | "混合型"}.
    """
    raw_input = state.get("raw_input", "")
    structured_info = state.get("structured_info")

    # Build optional structured_info summary for richer router context
    si_text = ""
    if structured_info is not None:
        tasks_str = "、".join(structured_info.tasks) if structured_info.tasks else ""
        outputs_str = "、".join(structured_info.outputs) if structured_info.outputs else ""
        si_text = f"任务：{tasks_str}；产出：{outputs_str}；进度：{structured_info.progress}"

    router = TemplateRouterAgent()
    template_type = router.classify(raw_input, structured_info_text=si_text)
    return {"template_type": template_type}
```
  </action>

  <verify>
    <automated>conda run -n llm-data-pipeline python -c "from workdiary_agent.router.agent import TemplateRouterAgent; from workdiary_agent.nodes.route_template import route_template_node; print('imports OK')"</automated>
  </verify>

  <acceptance_criteria>
    - workdiary_agent/router/__init__.py exists and contains "from .agent import TemplateRouterAgent"
    - workdiary_agent/router/agent.py exists and contains "class TemplateRouterAgent"
    - workdiary_agent/router/agent.py contains "class RouterState(TypedDict"
    - workdiary_agent/router/agent.py contains "analyze_content_node" and "decide_template_node"
    - workdiary_agent/router/agent.py contains "_builder.compile()" (sub-graph compiled at module level)
    - workdiary_agent/router/agent.py contains "def classify(self, raw_input: str"
    - workdiary_agent/nodes/route_template.py contains "from ..router.agent import TemplateRouterAgent"
    - workdiary_agent/nodes/route_template.py contains "router.classify("
    - `from workdiary_agent.router.agent import TemplateRouterAgent` succeeds (no import error)
    - The stub line `return {"template_type": "技术型"}` is no longer present in route_template.py
  </acceptance_criteria>

  <done>
    TemplateRouterAgent sub-graph created with analyze_content + decide_template nodes.
    route_template_node delegates to TemplateRouterAgent.classify(). All imports clean.
  </done>
</task>

</tasks>

<verification>
conda run -n llm-data-pipeline python -c "
from workdiary_agent.nodes.extract import extract_node
from workdiary_agent.router.agent import TemplateRouterAgent
from workdiary_agent.nodes.route_template import route_template_node
print('All Phase 02-02 imports OK')
"
</verification>

<success_criteria>
- extract_node uses ChatAnthropic.with_structured_output(StructuredInfo) and returns StructuredInfo
- TemplateRouterAgent is a compiled sub-graph with analyze_content → decide_template nodes
- route_template_node calls TemplateRouterAgent.classify() and writes template_type to state
- All imports resolve without error
- graph.py (unchanged) still builds successfully: `from workdiary_agent.graph import build_graph`
</success_criteria>

<output>
After completion, create .planning/phases/02-core-llm-nodes-and-template-routing/02-02-SUMMARY.md
</output>
